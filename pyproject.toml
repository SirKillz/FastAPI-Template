[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "quoting_brain" # < REPLACE THIS         
version = "0.0.1"
requires-python = ">=3.9"

[tool.hatch.build.targets.wheel]
packages = ["src/app_name"] #< REPLACE THIS